[package]
name = "virtio-blk"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
user_lib = { path = "../../lib" }
share = { path = "../../../share" }
volatile = "0.3"
log = "0.4"
bitflags = "1.3"
